import React from 'react';
const style = {
    display: "flex",
    justifyContent: 'center',
}
const Info = () => {
    return (
        <h1 style={style}>
            INFO
        </h1>
    );
};

export default Info;